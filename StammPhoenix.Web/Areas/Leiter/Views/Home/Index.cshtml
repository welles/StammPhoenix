<div class="card container p-0 shadow-sm">
    <h5 class="card-header">Mein Konto</h5>
    <div class="card-body">
        <div class="row">
            <div class="col-auto">
                <a class="btn btn-dark btn-flex" href="@Url.To("ChangePassword", "Konto", "Leiter", Url.To("Index", "Home", "Leiter"))" ><span class="material-icons">lock</span> Passwort ändern</a>
            </div>
            <div class="col-auto">
                <a class="btn btn-dark btn-flex" href="@Url.To("ChangeUsername", "Konto", "Leiter", Url.To("Index", "Home", "Leiter"))" ><span class="material-icons">person</span> Benutzernamen ändern</a>
            </div>
        </div>
    </div>
</div>
